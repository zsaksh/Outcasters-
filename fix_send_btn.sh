sed -i 's/if (inputText.isNotBlank() && !isGenerating) {/if (inputText.isNotBlank() \&\& !isGenerating) {\n                        val canSend = modelState is com.example.backend.models.ModelState.Active\n                        Box(\n                            modifier = Modifier\n                                .size(48.dp)\n                                .clip(CircleShape)\n                                .background(if (canSend) AccentTeal else MaterialTheme.colorScheme.surfaceVariant)\n                                .clickable(enabled = canSend) {\n                                    val text = inputText\n                                    inputText = ""\n                                    val currentMessagesSnapshot = messages.toList()/g' app/src/main/java/com/example/ui/screens/ChatScreen.kt
sed -i 's/.background(AccentTeal)/ /g' app/src/main/java/com/example/ui/screens/ChatScreen.kt
